#include <stdio.h> // Similar to summoning a library in Python
                   // Ergo import math, import random.
                   // #include <stdio.h> means summoning standard input ouput.

int main(void) // The "void" is the parameter we are passing. In this case, nothing.
{
    printf("Hello,world!\n"); // Most statements in C end with a semicolon.
    printf("Welcome to Intro to C");
}

//  $ gcc -o hello hello.c
    // Saves the file as a command known as "hello"
// $ ./hello
    // Executes the processes done in file.


